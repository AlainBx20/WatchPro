{
  "rules": {
    "presence": {
      "$roomId": {
        ".read": true,
        ".write": "auth != null",
        "$uid": {
          ".read": true,
          ".write": "auth != null && auth.uid === $uid"
        }
      }
    }
  }
}
